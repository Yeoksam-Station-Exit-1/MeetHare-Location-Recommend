spring:
  profiles:
    active: secret

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

kakao:
  key: ${kakaokey}

odsay:
  key: ${odsaykey}


